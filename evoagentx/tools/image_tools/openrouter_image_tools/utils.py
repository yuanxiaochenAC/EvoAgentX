"""Utilities kept minimal. Functions used across modules stay here.

Currently no shared helpers are required because OpenRouterImageGenerationEditTool
implements its own path/url conversion helpers. Add shared utilities here only
when they are used by multiple modules.
"""


